<%
// 以下の設定値を記入のこと
var pageinit = {
  title : '' ,
  keywords : '' ,
  description : '' ,
  ogurl : '' ,
  ogimage : '' ,
  path : '' ,
  shopify : 'true',
  category : ['contact'] ,
  pagename : '',
  css: [''],
  js:[],
  jsfunc:['']
};
%>
<% if(pageinit.shopify!=='true'){%>
<%- include(pageinit.path + 'assets/module/_head.ejs',{pageinit : pageinit}) %>
<%- include(pageinit.path + 'assets/module/_header.ejs',{pageinit : pageinit}) %>
<%- include(pageinit.path + 'assets/module/_menu.ejs',{pageinit : pageinit}) %>
<% } else {%>
{% layout 'theme.tui' %}
<% }%>
<div id="outerMenu">
  <%# ▼▼▼ここからページ内のHTMLを記載▼▼▼ %>
  <main role="main">
    <div id="container">
      <div class="secStyle_04 setMrg_27">
        <div class="inner">
          <section class="secStyle_06">
            <div class="inner">
              <div class="summaryStyle_05 setMrg_31">
                <p>tui公式サイトのご利用方法・商品やサービスについて何かご不明な点などございましたら、<br class="pc">お電話<span class="txStyle_04">（フリーダイヤル <a href="tel:0120-00-0000" class="linkStyle_01">0120-00-0000</a>）</span>または下記のフォームよりお気軽にお問い合わせください。<br>「<a href="{{ routes.root_url }}pages/faq" class="linkStyle_01">FAQ</a>」「<a href="{{ routes.root_url }}pages/rule" class="linkStyle_01">利用規約</a>」をご覧いただくと解決する場合がございます。</p>
              </div>
              <!-- /.summaryStyle_05 -->
              <div class="contact-form form-vertical">
                <% if(pageinit.shopify!=='true'){%>
                <form  id="ContactForm" accept-charset="UTF-8" class="contact-form">
                  <input type="hidden" name="form_type" value="contact">
                  <input type="hidden" name="utf8" value="✓">
                  <div class="grid grid--half-gutters">
                    <div class="grid__item medium-up--one-half">
                      <label for="ContactForm-name">名前<span aria-hidden="true">*</span></label>
                      <input type="text" id="ContactForm-name" required="" class="”required”" name="contact[名前]" value="">
                    </div>
                    <div class="grid__item medium-up--one-half">
                      <label for="ContactForm-email">Eメール<span aria-hidden="true">*</span></label>
                      <input type="email" id="ContactForm-email" name="contact[email]" autocorrect="off" autocapitalize="off" value="" aria-required="true">
                    </div>
                  </div>
                  <label for="ContactForm-phone">電話番号<span aria-hidden="true">*</span></label>
                  <input type="tel" id="ContactForm-phone" name="contact[電話番号]" pattern="[0-9\-]*" value="" aria-required="true">
                  <label for="ContactForm-message">お問い合わせ内容<span aria-hidden="true">*</span></label>
                  <textarea rows="10" required="" class="”required”" id="ContactForm-message" name="contact[メッセージ]"></textarea>
                  <div class="inputBtn">
                  <input type="submit" class="btn" value="送信">
                  </div>
                </form>
                <% } else {%>
                  {%- assign formId = 'ContactForm' -%}
                  {% form 'contact', id: formId %}
                    {% include 'form-status', form: form, form_id: formId %}

                    <div class="grid grid--half-gutters">
                      <div class="grid__item medium-up--one-half">
                        <label for="{{ formId }}-name">{{ 'contact.form.name' | t }}<span aria-hidden="true">*</span></label>
                        <input type="text" id="{{ formId }}-name" required class=”required” name="contact[{{ 'contact.form.name' | t }}]" value="{% if form[name] %}{{ form[name] }}{% elsif customer %}{{ customer.name }}{% endif %}">
                      </div>
                      <div class="grid__item medium-up--one-half">
                        <label for="{{ formId }}-email">{{ 'contact.form.email' | t }} <span aria-hidden="true">*</span></label>
                        <input
                          type="email"
                          id="{{ formId }}-email"
                          name="contact[email]"
                          autocorrect="off"
                          autocapitalize="off"
                          value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}"
                          aria-required="true"
                          {%- if form.errors contains 'email' -%}
                            class="input--error"
                            aria-invalid="true"
                            aria-describedby="{{ formId }}-email-error"
                          {%- endif -%}
                          >
                        {%- if form.errors contains 'email' -%}
                          <span id="{{ formId}}-email-error" class="input-error-message">
                            <span class="visually-hidden">{{ 'general.accessibility.error' | t }} </span>
                            {% include 'icon-error' %}
                            <span>{{ form.errors.translated_fields['email'] | capitalize }} {{ form.errors.messages['email'] }}.</span>
                          </span>
                        {%- endif -%}
                      </div>
                    </div>

                    <label for="{{ formId }}-phone">{{ 'contact.form.phone' | t }}</label>
                    <input type="tel" id="{{ formId }}-phone" name="contact[{{ 'contact.form.phone' | t }}]" pattern="[0-9\-]*" value="{% if form[phone] %}{{ form[phone] }}{% elsif customer %}{{ customer.phone }}{% endif %}">

                    <label for="{{ formId }}-message">お問い合わせ内容<span aria-hidden="true">*</span></label>
                    <textarea rows="10" required class=”required” id="{{ formId }}-message" name="contact[{{ 'contact.form.message' | t }}]">{% if form.body %}{{ form.body }}{% endif %}</textarea>
                    <div class="inputBtn">
                    <input type="submit" class="btn" value="{{ 'contact.form.submit' | t }}">
                    </div>

                  {% endform %}
                <% }%>
              </div>
            </div>
            <!-- /.inner -->
          </section>
          <!-- /.secStyle_06 -->
        </div>
        <!-- /.inner -->
      </div>
      <!-- /.secStyle_04 -->
    </div>
    <!-- /#container -->
  </main>
  <%# ▲▲▲ここまでページ内のHTMLを記載▲▲▲ %>
  <% if(pageinit.shopify!=='true'){%>
  <%- include(pageinit.path + 'assets/module/_footer.ejs',{pageinit : pageinit}) %>
  <% } else {%>
  {% include 'footer-tui' %}
  <% }%>
</div>
<!-- /#outerMenu -->
<%- include(pageinit.path + 'assets/module/_foot.ejs',{pageinit : pageinit}) %>
<% if(pageinit.shopify!=='true'){%>
</body>
</html>
<% }%>
